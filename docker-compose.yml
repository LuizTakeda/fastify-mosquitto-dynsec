version: "3.8"

services:
  mosquitto:
    image: eclipse-mosquitto:2.0
    container_name: fastify-mosquitto-broker
    ports:
      - "1883:1883"
    environment:
      MOSQUITTO_DYNSEC_PASSWORD: admin123456789
    volumes:
      - ./mosquitto/config:/mosquitto/config:ro
